Hello,
Today we installed a package inside the conda environment (using pip install --editable .)

I have added read_pulse, find_peaks, and calc_heart_rate functions to the package and called them in this session's notebook. There, I also plotted the data to test the functions. However, I am not sure what "in your notebook use spacemed.load_pulse function" exactly means. Do I need this when I start a new notebook? I can only use print(aFunction(1,2)) to call a function. 
Are my functions now fully usable in terms of the __init and __version files? Do I change the version manually, whenever I feel inclined?
I hope my questions are not too unreasonable.