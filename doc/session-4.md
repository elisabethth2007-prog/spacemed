Hello.
This session we learned more about using code, markdown and raw cells in the notebook. We also learned to 
make Reproducible Documents with quarto (based on R markdown) and render the document.
Then we were using numpy and matplotlib on our oxymetry sensor data. After converting Absorption and
Time into arrays and performing some exercises on arrays, we developed an algorithm to find the peaks.
(I think I need more practice with the arrays.)
Next, this data was used to compute the HR, using the formula:
HR= 60/ delta Tpeaks (bpm)
The algorithm building was very interesting.

Question: Can we maybe discuss next session what is turned into a pdf using quarto render? Just a quick recap of what will be shown in the final document and what remains invisible?
