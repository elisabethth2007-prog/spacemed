Hello to the next markdown!
This session was dedicated to creating executable scripts, in this case uniting our predefined functions in a plot_heart_rate.py file.
This script was then added to our terminal commands by modifying the setup.cfg file. It can be called with: sm_plot_heart

The parser then translates terminal input into structured Python variables the program can use, with the arguments data, --output, and --version.

Finally, we talked about testing and applied flake8 (a linter) to detect style and syntax errors.


